import sys
from pathlib import Path
import numpy as np

# Add Python Model to path so we can import variables if needed, 
# but it's probably easier to just copy the necessary parts or run the script and hook it.
